version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: armt
      POSTGRES_USER: armt
      POSTGRES_PASSWORD: armt_local
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
